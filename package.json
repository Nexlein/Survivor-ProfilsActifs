{
  "name": "survivor-profilsactifs",
  "private": true,
  "description": "Video-based professional networking platform for the Ministère du Job et Bonheur",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "setup": "npm install && npm run db:start && cd backend && npx prisma generate",
    "db:start": "docker compose -f backend/docker-compose.yml up -d",
    "db:stop": "docker compose -f backend/docker-compose.yml down",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev": "npm run db:start && concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
